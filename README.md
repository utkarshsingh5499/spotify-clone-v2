# Spotify Clone V2

A Spotify-style music dashboard clone built with HTML, CSS, and JavaScript.

## Project Files

- `index.html` contains the app layout.
- `src/components/Player.jsx` is currently reserved for player component work.

## Note

`index.html` currently references `styles.css` and `app.js`, which are not yet included in this folder.
